{{ if .Vars.run_heavy_tests }}
command:
  repos_configured:
  {{ if .Vars.rhel8cis_rule_1_2_5 }}
    title: 1.2.5 Ensure Package manager repositories are configured
    exit-status: 0
    exec: "yum repolist"
  {{ end }}
{{ end }}
