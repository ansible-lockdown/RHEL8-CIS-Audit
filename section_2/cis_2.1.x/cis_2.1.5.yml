---

{{ if .Vars.rhel8cis_level_1 }}
  {{ if .Vars.rhel8cis_rule_2_1_5 }}
    {{ if not .Vars.rhel8cis_dns_server }}
      {{ if not .Vars.rhel8cis_dns_mask }}
package:
  dns_pkg:
    title: 2.1.5 | Ensure dns server services are not in use | pkg removed
    name: bind
    installed: false
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.1.5
      NIST800-53R5:
      - CM-7
      {{ end }}
      {{ if .Vars.rhel8cis_dns_mask }}
file:
  dns_service_masked:
    title: 2.1.5 | Ensure dns server services are not in use | masked
    path: /etc/systemd/system/named.service
    exists: true
    filetype: symlink
    linked-to: /dev/null
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.1.5
      NIST800-53R5:
      - CM-7
      {{ end }}
    {{ end }}
  {{ end }}
{{ end }}
