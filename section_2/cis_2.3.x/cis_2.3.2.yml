---

{{ if .Vars.rhel8cis_level_1 }}
  {{ if .Vars.rhel8cis_rule_2_3_2 }}
file:
  chrony_servers_pools:
    title: 2.3.2 | Ensure chrony is configured | server
    path: /etc/chrony.conf
    exists: true
    contents:
    - '/^(server|pool)\s.*/'
    skip: false
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.3.2
      NIST800-53R5:
      - AU-3
      - AU-12
  {{ end }}
{{ end }}
