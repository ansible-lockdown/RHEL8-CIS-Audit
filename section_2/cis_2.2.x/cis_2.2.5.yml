---

{{ if .Vars.rhel8cis_level_1 }}
  {{ if not .Vars.rhel8cis_tftp_client }}
    {{ if .Vars.rhel8cis_rule_2_2_5 }}
package:
  tftp_client:
    title: 2.2.5 | Ensure tftp client is not installed
    installed: false
    name: tftp
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 2.2.5
      NIST800-53R5:
      - CM-7
    {{ end }}
  {{ end }}
{{ end }}
