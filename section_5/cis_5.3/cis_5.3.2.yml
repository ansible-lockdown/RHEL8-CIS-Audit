{{ if .Vars.rhel8cis_rule_5_3_2 }}
command:
  authselect_profile:
    title: 5.3.2 Select authselect profile
    exec: 'authselect current'
    exit-status: 0
    stdout:
    - '/^Profile ID: {{ .Vars.rhel8cis_authselect_profile_id }}/'
    - '!/No existing configuration detected/'
{{ end }}
