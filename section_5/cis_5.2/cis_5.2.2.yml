{{ if ((.Vars.rhel8cis_rule_5_2_2) and .Vars.rhel8cis_sshd.limited) }}
file:
  /etc/ssh/sshd_config:
    title: 5.2.2 Ensure SSH access is limited 
    exists: true
    contains:
    {{ range .Vars.rhel8cis_sshd_access }}
    - {{ . }}
    {{ end }}
{{ end }}
