---

{{ if .Vars.rhel8cis_level_1 }}
  {{ if .Vars.rhel8cis_rule_7_1_10 }}
file:
  /etc/opasswd_perms:
    title: 7.1.10 | Ensure permissions on /etc/opasswd are configured
    exists:
      or:
      - true
      - false
    path: /etc/opasswd
    mode:
      or:
      - '0644'
      - '0640'
      - '0600'
      - '0400'
    owner: root
    group: root
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 7.1.10
      NIST800-53R5:
      - AC-3
      - MP-2
  {{ end }}
{{ end }}
