file:
  {{ if .Vars.rhel8cis_rule_6_1_6 }}
  /etc/passwd:
    title: 6.1.6 Ensure permissions on /etc/passwd- are configured
    exists: true
    mode: '0600'
    owner: root
    group: root
  {{ end }}

