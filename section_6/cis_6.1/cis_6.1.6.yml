file:
  {{ if .Vars.rhel7cis_rule_6_1_6 }}
  /etc/passwd:
    title: 6.1.6 Ensure permissions on /etc/passwd- are configured
    exists: true
    mode: '0644'
    owner: root
    group: root
  {{ end }}

