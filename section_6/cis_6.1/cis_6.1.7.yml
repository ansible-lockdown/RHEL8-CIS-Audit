{{ if .Vars.rhel8cis_rule_6_1_7 }}
command:
  shadow-_perms:
    title: 6.1.7 | L1 | Ensure permissions on /etc/shadow- are configured
    exec:  stat -L -c "%a %U %G" /etc/shadow-
    exit-status: 0
    stdout:
    -  '/(0|[4,6][0,4]0) root root/'
{{ end }}
