file:
  {{ if .Vars.rhel8cis_rule_6_1_3 }}
  /etc/passwd:
    title: 6.1.3 Ensure permissions on /etc/shadow are configured
    exists: true
    mode: '0000'
    owner: root
    group: root
  {{ end }}

