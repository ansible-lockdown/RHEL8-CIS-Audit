file:
  {{ if .Vars.rhel8cis_rule_6_1_5 }}
  /etc/passwd:
    title: 6.1.5 Ensure permissions on /etc/gshadow are configured
    exists: true
    mode: '0640'
    owner: root
    group: root
  {{ end }}

