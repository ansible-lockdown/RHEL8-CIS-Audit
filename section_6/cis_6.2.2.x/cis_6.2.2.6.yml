---

{{ if .Vars.rhel8cis_level_1 }}
  {{ if .Vars.rhel8cis_rule_6_2_2_6 }}
    {{ if not .Vars.rhel8cis_remote_log_server }}
command:
  remote_syslog:
    title: 6.2.2.6 | Ensure rsyslog is configured to send logs to a remote host
    exec: 'grep -E "action.*omfwd.*target" /etc/rsyslog.conf /etc/rsyslog.d/*.conf'
    exit-status:
      or:
        - 0
        - 2
    stdout:
    - '*.* action(type="omfwd" target="{{ .Vars.rhel8cis_remote_log_host }}" port="{{ .Vars.rhel8cis_remote_log_port }}"'
    - protocol="{{ .Vars.rhel8cis_remote_log_protocol }}"
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 6.2.2.6
      NIST800-53R5:
      - AU-6
    {{ end }}
  {{ end }}
{{ end }}
