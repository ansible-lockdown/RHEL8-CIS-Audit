{{ if .Vars.rhel7cis_rule_6_2_17 }}
command:
  no_dup_groupname:
    title: Ensure no duplicate igroup names exist
    exec: "cat /etc/group | cut -d: -f1 | uniq -d"
    exit-status: 0
    stdout:
    - '!/./'
{{ end }}
