{{ if .Vars.rhel7cis_rule_6_2_15 }}
command:
  no_dup_gid:
    title: Ensure no duplicate GIDs exist
    exec: "cat /etc/group | cut -d: -f3 | uniq -d"
    exit-status: 0
    stdout:
    - '!/./'
{{ end }}
